Flow:                         1.000  mol/s                  63.791  kg/h



name
val.split('.')
spaces = 31 - len(name) - len(val.split('.')) +  ....
dot on 31
three digits

dot on 62
three digits

3.2
    elif valueSI == None and value >= 0:
        val = round(decimal.Decimal(value),3)
        #format print out
        fldob = str('{0:22}' + '{1:>9,}.' + '{2}' + '{3:18}\n').format(
            name,
            math.floor(val), str(val - math.floor(val))[2:],
            unit)


2.7
 elif valueSI == None and value >= 0:
        val = round(decimal.Decimal(value),3)
        #format print out
        fldob = unicode(u'{0:22}' + u'{1:>9,}.' + u'{2}' + u'{3:18}\n').format(
            name,
            math.floor(val), unicode(val - math.floor(val))[2:],
            unit)
